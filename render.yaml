services:
  - type: web
    name: sovary-os
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn sovary_server:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.0
